
{{ form_start(form, {'action': path('actividad_new')}) }}


    {{ form_start(form) }}




{{ form_widget(form) }}
<input type="submit" value="Create" />
{{ form_end(form) }}

<ul>
    <li>
        <a href="{{ path('actividad_index') }}">Back to the list</a>
    </li>
</ul>

